server:
  port: 9000

spring:
  application:
    name: tuanpet-thirdParty
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #????????
      config:
        server-addr: localhost:8848 #??????
        file-extension: properties

